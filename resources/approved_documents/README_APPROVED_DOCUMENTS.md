# Approved documents for NHIS Assistant V2

Place approved PDF, DOCX, and TXT resources in this folder, then run:

```bash
python scripts/build_resource_index.py
```

The generated local retrieval index is written to `resources/generated/faq_index_seed.json`.
